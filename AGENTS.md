# AGENTS

本文件是仓库内所有模型/代理的唯一执行规范。

## 1. 优先级
- 禁止为了迎合用户，放弃自己的正确意见
- 高风险与边界约束最高；需先确认的操作不得自行跳过。
- 用户明确指示高于其他默认规则，但仅决定“做什么”；执行时仍须遵守本规范中的范围、记录、文档、编码保护和高风险确认要求。

## 2. 执行
- 进入仓库先读根目录 `AGENTS.md`。
- 按用户明确指示直接执行，不自动选任务、不排程、不自行决定下一任务。
- 用户指定文件、对象或交付物后，只围绕当前对象执行；除非用户明确要求，否则不额外做任务发现、拆分、推荐或扩展到其他对象。
- 多任务按用户顺序逐个完成“实现、验证、日志追加”闭环后再进入下一个；若顺序有明显依赖风险，先提示再调整。
- 只做当前任务 `scope` 内的改动；如需跨 `scope`，须用户明确要求，并在汇报与日志中说明。

## 3. 编码与施工原则
### 编码前先思考

**不要妄下断言。不要掩饰困惑。把取舍讲清楚。**

实施前：
- 请明确陈述您的假设。如有疑问，请提出。
- 如果存在多种解释，请将它们提出来，不要默默地做出选择。
- 如果存在更简单的方法，请提出来。必要时要坚持自己的正确判断，不得为了迎合而采用更差方案。
- 如果有什么不清楚的地方，停下来。说出让你困惑的地方。然后提问或补充查证。
- 信息不足以支撑实施时，不得靠猜测补齐需求。

### 简单优先

**用最少的代码解决问题。不做任何推测性设计。**

- 不增加超出要求的功能。
- 不为一次性代码进行抽象。
- 不提供任何未被要求的“灵活性”或“可配置性”。
- 未出现明确需求、真实复用证据或可复现问题前，不得为低概率场景、假想未来需求或理论上的完整性增加分支、抽象或防御性代码。
- 实现以满足当前需求、便于理解和验证为准，不以覆盖假想边界或未来扩展为目标。
- 对没有现实发生依据的情况，不预先补错误处理。
- 如果 50 行能完成，就不要写成 200 行；若已写复杂，必须主动回退到更简单方案。

问问自己：“一位资深工程师会认为这过于复杂吗？”如果答案是肯定的，那就继续简化。

### 精准改动

**只改必须改的地方。只清理你自己造成的尾巴。**

编辑现有代码时：
- 不要“改进”相邻的代码、注释或格式。
- 不要重构没有问题的代码。
- 即使你的做法不同，也要保持与现有风格一致。
- 如果你发现无关的死代码，请指出来，不要删除它。

当你的改动留下孤儿项时：
- 删除因本轮修改而不再使用的导入项、变量、函数。
- 除非用户明确要求，否则不要删除已有的无效代码，也不要扩大清理范围。

判断标准：每一行改动后的代码，都应该直接追溯到用户的请求。

### 目标驱动执行

**定义成功标准。循环直至验证通过。**

将任务转化为可验证的目标：
- “添加验证” -> “补针对无效输入的验证，并确认全部通过”
- “修复漏洞” -> “先构造能够稳定复现该漏洞的验证，再修复并确认通过”
- “重构 X” -> “确保重构前后测试都通过”

对于多步骤任务，请简要说明计划：
```text
1. [步骤] -> verify: [检查项]
2. [步骤] -> verify: [检查项]
3. [步骤] -> verify: [检查项]
```

如果成功标准仍然模糊，不得直接开工，必须先澄清。

强成功标准能让你独立循环迭代。
弱标准（如“先做出来再说”）会让你不断回头澄清。

## 4. 验证、记录与落点
- 进度日志统一使用 `progress.md`，仅在末尾追加，不改写历史记录。
- 纯问答、只读检查、配置解释、方案讨论不视为“代码改动任务”；除非用户明确要求落文件，否则不强制更新进度日志或 `docs/`。
- 进度日志只在以下情况追加：实际修改仓库文件（包括创建、编辑、删除、移动、重命名文件或目录）、生成正式交付文档、或用户明确要求记录。
- 实际改动必须有对应验证；验证证据写入进度日志的 `### Testing`。若无可信验证入口，须明确说明缺口，不得把“未验证”说成“已可用”。
- 行为、配置、依赖发生正式改动，且该改动影响后续使用、部署、验证或协作时，必须同步更新 `docs/`；根目录必须存在 `docs/`，扩展文档统一放此目录。
- 每完成一个需记录的任务追加一轮，多任务则连续追加，固定格式：
  - `## YYYY-MM-DD - Task: <description>`
  - `### What was done`：只写本轮完成了什么，按业务动作或结果概括，不按文件逐个展开。
  - `### Testing`
  - `### Notes`：必须列出改动文件清单，每个文件后用一句简要说明本轮改了什么；必须给出可执行的回滚方式或回滚点。

## 5. 高风险与边界
- 禁止全量格式化、全量转码、批量无差别替换。
- 改核心协议、内存地址、数据库结构须用户明确批准。
- 网络包处理、线程并发、鉴权/反作弊路径的改动须先得到用户确认。
- 尊重任务范围约束，保持文件原编码，避免破坏性操作。

## 6. 回复方式
- 本节固定回复结构主要用于阶段性汇报、施工结论、审稿结论；简单问答可直接短答，但必须先给明确结论。
- 默认面向业务人员、方案负责人、项目负责人，使用业务语言描述影响，采用便于快速判断、拍板、推进的汇报口吻。
- 先讲结论，再讲原因、风险、下一步。用户问“能不能做/施工/继续/是否通过”时，第一句直接给明确结论，不让用户从分析里猜。
- 默认优先回答四件事：现在能不能推进、卡点是什么、如果现在继续会出什么问题、下一步最小动作是什么。
- 固定结构输出：
  - `业务结论`
  - `原因`：只写影响结论的关键点，`2` 到 `4` 条。
  - `风险`：说明“如果现在就做，会出什么问题”。
  - `下一步`：只写最小可执行动作，不一次给过大方案。
- 每部分用短段落，先说明意思再补一句解释；不只给结论，也不堆细节。
- 阶段性汇报默认含两部分：刚完成了什么、下一步计划做什么；以业务场景和业务逻辑表述，保持简洁。

## 7. 技术依据
- 用户未明确要求源码细节时，函数名、类名、调用链、文件列表、行号只作补充依据，不放开头、不作主要表达。
- 仅在以下情况主动展开技术细节：用户明确要求、需证明结论不是主观判断、不给依据就无法支持结论。
- 同时给结论与依据时，先业务判断再技术依据；技术依据统一放最后，标题写为 `技术依据（按需阅读）`。
- 禁止为显得严谨而先输出大段逐文件、逐函数、逐调用链分析。

## 8. 工具与并行协作
- 工具选择以最小有效路径为准；代码开发、源码搜索、补丁编辑、测试执行和常规工程验证默认使用 Codex 自带工具。
- `desktop-commander` 仅用于跨盘/非工作区文件、目录整理，以及 PDF/Word/Excel 等本机文档处理；`Serena` 仅用于符号、引用、调用关系、结构概览和精确重命名等语义级代码理解。
- 多智能体并行修改须先划分独立文件或模块责任边界；同一文件默认串行执行。

## 8.1 Token 与上下文节省
- 以最小有效路径完成任务：简单文案、单文件小改、已知路径读取和针对性验证，不得为此启动子代理或 AgentSwarm。
- 只有在任务确实需要独立调查、跨模块分析或大范围实现时才使用子代理；默认最多使用一个 `explore` 或一个 `coder`，不得让子代理无必要地继续派生子代理。
- 并行子代理必须有明确、互不重叠的文件或模块边界；不能用并行重复读取同一批文件来“多角度确认”。每个子代理提示必须要求只返回关键文件、结论、缺口和验证命令，禁止复述背景或输出长篇教程。
- 需求未稳定、规则未测试通过前，不得同时大规模实现服务端协议、前端状态、动画、资源和文档；先锁定短规格与服务端测试，再做前端和视觉，避免整批返工。
- 已完成调查或实现的结论应通过短交接记录复用，不得把完整历史分析、旧计划和大段工具输出复制到后续提示词；后续代理应直接读取当前文件和短计划。
- 独立阶段完成后及时压缩上下文或开启新会话；新会话交接只保留目标、当前状态、允许修改文件、验证结果和未决问题。不得让已废弃的方案继续占用上下文。
- 读取文件必须按需、按行范围和按模块进行；禁止无目的读取整个大型文件、`node_modules`、构建产物、日志或会话 wire 文件。统计目录大小时必须排除 `node_modules`、`.next`、`dist`、`build` 和 `.git`，避免无效扫描。
- 验证命令按改动范围选择：先跑最接近的测试或构建，只有跨模块改动或发布前才跑全套；同一轮不得无故重复成功的验证。
- 代理开始任务前应先确认“本轮只解决什么、成功标准是什么、允许修改哪些文件”；超出范围必须先说明，不得自行扩展。
- 如需判断 Token 或额度，优先使用 CLI `/usage` 和官方控制台；不得通过扫描本地会话日志猜测计费，除非用户明确要求且已确认敏感信息风险。

## 9. 图像生成
- 用户明确要求生成或重绘图片时，代理应直接调用仓库内 `tools/generate-assets.mjs` 完成交付，不只提供提示词，也不要求用户切换当前推理模型。
- 当前推理模型负责理解需求、整理提示词和调用工具；实际图片统一使用 Gemini 原生图片接口，环境变量为 `GEMINI_BASE_URL`/`GEMINI_API_KEY`。支持 `gemini-3.1-flash-image` 与 `gemini-3-pro-image-preview`，默认使用后者；禁止回退到 `gpt-image-2` 或 OpenAI 图片接口。
- Gemini 请求必须使用 `${GEMINI_BASE_URL}/v1beta/models/<model>:generateContent`。如果配置的 Base URL 末尾带 `/v1`，调用前必须去掉 `/v1`，避免形成错误的 `/v1/v1beta`；Base URL 的主机和代理路径保持现有配置不变。
- 默认遵循 `docs/asset-generation-workflow.md`；角色提示词同时遵循 `docs/new-gorilla-generation-prompts.md`。默认生成参数为 `IMAGE_MODEL=gemini-3-pro-image-preview`、`IMAGE_SIZE=1024x1536`、`IMAGE_QUALITY=high`，用户另有要求时只允许在两个 Gemini 图片模型中选择。
- API 密钥只能通过运行环境的 `GEMINI_API_KEY` 提供，绝不写入 `AGENTS.md`、文档、源码、命令示例或日志；不读取、展示、复制或改写 `.env` 中的密钥。图像 API 不可用时应报告实际错误，不得通过切换推理模型掩盖端点故障。
- 候选图先写入 `assets/source/`，未经用户确认不得覆盖旧图、写入 `durian-web/public/assets/` 或加入正式牌组。
- 新猩猩正式上线时，必须制作 `750x1000 RGB` 标准卡片，并从定稿角色的精彩区域截取、提炼或重绘为 `256x256 RGB` 聊天表情；禁止直接把整张卡面缩小充当表情。
- 每位正式员工必须加入员工手册，写明名称、称号、能力、所属模式、正式 ID 和符合角色职业与机制的故事；同时更新卡池历史、资源清单、生成流程等本地文档。
- 上线前必须按用户要求明确角色加入经典模式、猩风作浪或其他指定模式，不得自行扩大模式范围。
- 在职角色总数超过 7 位时，大厅角色按房间生成随机顺序分配；角色数量足够覆盖玩家时，玩家之间不得重复分配角色。
